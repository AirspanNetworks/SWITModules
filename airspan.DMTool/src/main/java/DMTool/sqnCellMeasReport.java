/*  -*- java -*- ****************************************************************** * * Module  : DCC - Device Controller Client in Java for Sequans devices * * Purpose : THIS FILE IS AUTOMATICALLY GENERATED ! * ***************************************************************** * *  Copyright (c) 2009 SEQUANS Communications. *  All rights reserved. *   *  This is confidential and proprietary source code of SEQUANS *  Communications. The use of the present source code and all *  its derived forms is exclusively governed by the restricted *  terms and conditions set forth in the SEQUANS *  Communications' EARLY ADOPTER AGREEMENT and/or LICENCE *  AGREEMENT. The present source code and all its derived *  forms can ONLY and EXCLUSIVELY be used with SEQUANS *  Communications' products. The distribution/sale of the *  present source code and all its derived forms is EXCLUSIVELY *  RESERVED to regular LICENCE holder and otherwise STRICTLY *  PROHIBITED. * ***************************************************************** */package DMTool;public class sqnCellMeasReport {	public boolean servingValid;	public AsGenericCellMeasInfo servingMeas;	public AsGenericCellMeasInfo[] neigh;	public sqnCellMeasReport() {		// Initialization		// servingMeas		servingMeas = new AsGenericCellMeasInfo();	}	public static int marshall(byte[] buf, final sqnCellMeasReport p, OutValue<Integer> _offset) {		int st = Status.STATUS_OK;		// Check the size of the fundamental types of the structure		if (buf.length < (_offset._value+8)) return DccError.ERR_OVERFLOW;		// servingValid		Util.Copy32ToBuffer(buf, p.servingValid, _offset);		// servingMeas		if ( Status.STATUS_OK != (st = AsGenericCellMeasInfo.marshall(buf, p.servingMeas, _offset)) ) return st;		// neighQty		if(p.neigh == null)			Util.Copy32ToBuffer(buf, 0, _offset);		else {			Util.Copy32ToBuffer(buf, p.neigh.length, _offset);			// neighArray			for (int i = 0; i<p.neigh.length; i++) {				if ( Status.STATUS_OK != (st = AsGenericCellMeasInfo.marshall(buf, p.neigh[i], _offset)) ) return st;			}		}		return st;	}	public static int unmarshall(final byte[] buf, sqnCellMeasReport p, OutValue<Integer> _offset) {		int st = Status.STATUS_OK;		// Check the size of the fundamental types of the structure		if (buf.length < (_offset._value+8)) return DccError.ERR_OVERFLOW;		// servingValid		p.servingValid = Util.Copy32FromBuffer(buf, _offset) == 0? false:true;		// servingMeas		if ( Status.STATUS_OK != (st = AsGenericCellMeasInfo.unmarshall(buf, p.servingMeas, _offset)) ) return st;		// Array		{			// neighQty			int arrSize = (int) Util.Copy32FromBuffer(buf, _offset);			// neighArray			p.neigh = new AsGenericCellMeasInfo[arrSize];			for (int i = 0; i<p.neigh.length; i++) {				p.neigh[i] = new AsGenericCellMeasInfo();			}			for (int i = 0; i<p.neigh.length; i++) {				if ( Status.STATUS_OK != (st = AsGenericCellMeasInfo.unmarshall(buf, p.neigh[i], _offset)) ) return st;			}		}		return st;	}}