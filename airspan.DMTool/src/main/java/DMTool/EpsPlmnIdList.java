/*  -*- java -*- ****************************************************************** * * Module  : DCC - Device Controller Client in Java for Sequans devices * * Purpose : THIS FILE IS AUTOMATICALLY GENERATED ! * ***************************************************************** * *  Copyright (c) 2009 SEQUANS Communications. *  All rights reserved. *   *  This is confidential and proprietary source code of SEQUANS *  Communications. The use of the present source code and all *  its derived forms is exclusively governed by the restricted *  terms and conditions set forth in the SEQUANS *  Communications' EARLY ADOPTER AGREEMENT and/or LICENCE *  AGREEMENT. The present source code and all its derived *  forms can ONLY and EXCLUSIVELY be used with SEQUANS *  Communications' products. The distribution/sale of the *  present source code and all its derived forms is EXCLUSIVELY *  RESERVED to regular LICENCE holder and otherwise STRICTLY *  PROHIBITED. * ***************************************************************** */package DMTool;public class EpsPlmnIdList {	public short numOfPlmnIds;	public short[] pad = new short[3];	public EpsPlmnIdTag[] plmnId = new EpsPlmnIdTag[16];	public EpsPlmnIdList() {		// Initialization		// plmnId		for (int i = 0; i<plmnId.length; i++) {			plmnId[i] = new EpsPlmnIdTag();		}	}	public static int marshall(byte[] buf, final EpsPlmnIdList p, OutValue<Integer> _offset) {		int st = Status.STATUS_OK;		// Check the size of the fundamental types of the structure		if (buf.length < (_offset._value+1)) return DccError.ERR_OVERFLOW;		// numOfPlmnIds		Util.Copy8ToBuffer(buf, p.numOfPlmnIds, _offset);		// pad		for (int i = 0; i<3; i++) {			Util.Copy8ToBuffer(buf, p.pad[i], _offset);		}		// plmnId		for (int i = 0; i<16; i++) {			if ( Status.STATUS_OK != (st = EpsPlmnIdTag.marshall(buf, p.plmnId[i], _offset)) ) return st;		}		return st;	}	public static int unmarshall(final byte[] buf, EpsPlmnIdList p, OutValue<Integer> _offset) {		int st = Status.STATUS_OK;		// Check the size of the fundamental types of the structure		if (buf.length < (_offset._value+1)) return DccError.ERR_OVERFLOW;		// numOfPlmnIds		p.numOfPlmnIds = (short) Util.Copy8FromBuffer(buf, _offset);		// pad		for (int i = 0; i<3; i++) {			p.pad[i] = (short) Util.Copy8FromBuffer(buf, _offset);		}		// plmnId		for (int i = 0; i<16; i++) {			if ( Status.STATUS_OK != (st = EpsPlmnIdTag.unmarshall(buf, p.plmnId[i], _offset)) ) return st;		}		return st;	}}